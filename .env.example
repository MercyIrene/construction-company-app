# Copy to apps/web/.env.local for development.
# Public values (safe in the browser):
NEXT_PUBLIC_SUPABASE_URL=https://golnseayakztktwpdlhc.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_Q5lYiihjnv80JfFwyVTUJQ_kIhE2PRa

# Server-only. Get from Supabase dashboard → Project Settings → API keys.
# Required for all write paths (the app writes through the service layer,
# never through client keys — see docs/10 §3 and ADR-0003).
SUPABASE_SECRET_KEY=
